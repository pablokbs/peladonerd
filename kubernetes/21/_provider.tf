# Configurando el provider de Upcloud 

provider "upcloud" {
  # Este provider no necesita opciones, solamente exportar las credenciales en tus variables de entorno
  # usando .bashrc, .zshrc o similar
  # export UPCLOUD_USERNAME="Username de user Upcloud API"
  # export UPCLOUD_PASSWORD="Password del user Upcloud API"
}